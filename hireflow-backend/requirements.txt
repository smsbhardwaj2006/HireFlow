Django==5.0.6
djangorestframework==3.15.1
djangorestframework-simplejwt==5.3.1
django-cors-headers==4.3.1
django-filter==24.2
python-dotenv==1.0.1

# MySQL support for production (DJANGO_ENV=production) — not needed for
# local SQLite development, but listed so production installs work too.
psycopg2-binary==2.9.9
dj-database-url==2.2.0

# Deployment
gunicorn==22.0.0
whitenoise==6.7.0
